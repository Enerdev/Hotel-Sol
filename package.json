{
  "name": "hotel-management-system",
  "version": "1.0.0",
  "description": "Sistema integral de gestión hotelera",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "check-setup": "node check-setup.js",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd backend && npm run build && cd ../frontend && npm run build && cd .. && rm -rf backend/public && mkdir -p backend/public && cp -r frontend/dist/. backend/public/ && rm -f backend/public/.htaccess",
    "start:debug": "node backend/dist/index.js",
    "diag:logs": "echo '--- Buscando logs ---'; find /var/www/vhosts -iname '*error_log*' 2>/dev/null | grep -i hotel || echo 'no encontrado con find'; echo '--- Procesos node activos ---'; ps aux | grep '[n]ode' || echo 'sin salida de ps'; echo '--- Puerto 3000 ---'; ss -ltnp 2>/dev/null | grep 3000 || netstat -ltnp 2>/dev/null | grep 3000 || echo 'sin ss/netstat'",
    "kill:debug": "pkill -f 'backend/dist/index.js' && echo 'proceso de debug detenido' || echo 'no habia proceso corriendo'",
    "diag:full": "echo '--- ls raiz ---'; ls -la .; echo '--- ls backend ---'; ls -la backend; echo '--- ls backend/dist ---'; ls -la backend/dist 2>&1; echo '--- existe backend/node_modules? ---'; ls backend/node_modules 2>&1 | head -3; echo '--- existe node_modules raiz? ---'; ls node_modules 2>&1 | head -3; echo '--- existe backend/.env? ---'; ls -la backend/.env 2>&1; echo '--- buscando carpeta de logs del dominio ---'; find /var/www/vhosts/system -maxdepth 2 -iname '*hotel*' 2>/dev/null; echo '--- probando el arranque a mano con timeout 5s ---'; timeout 5 node backend/dist/index.js 2>&1 || echo '(el proceso se corto solo, revisa arriba si imprimio algo antes)'",
    "deploy:plesk": "npm install && npm run build",
    "test:db": "cd backend && npm run test:db",
    "seed:passwords": "cd backend && npm run seed:passwords",
    "lint": "cd backend && npm run build && cd ../frontend && npm run lint",
    "clean": "rm -rf backend/dist frontend/dist backend/node_modules frontend/node_modules node_modules",
    "test": "cd backend && npm run test:db"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "husky": "^9.0.11"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "repository": {
    "type": "git",
    "url": "."
  },
  "keywords": [
    "hotel",
    "management",
    "system",
    "react",
    "nodejs",
    "typescript",
    "mysql"
  ],
  "author": "Hotel Management Team",
  "license": "UNLICENSED"
}